<resources>

    <!-- 弹出Dialog样式 -->

    <!-- 没有标题样式 -->
    <style name="no_title" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
    </style>
    <!-- dialog弹出和退出动画 -->
    <!-- 相机图片设置字体为黑色 -->

    <!-- 闲心布局输入框样式 -->
  <!--  <style name="line_input_style">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@mipmap/enter_box</item>
        <item name="android:orientation">horizontal</item>
    </style>-->
    <!-- 输入框样式 -->


    <!-- RadioButton样式 -->
   <!-- <style name="radio_style">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">15sp</item>
        <item name="android:background">@drawable/rb_list</item>
    </style>-->

    <!--主界面下面的菜单选择样式-->
    <!--<style name="main_home_tab_style">
        <item name="android:layout_width">0dip</item>
        <item name="android:layout_height">fill_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:button">@null</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:drawablePadding">5dip</item>
        <item name="android:paddingTop">2dip</item>
        <item name="android:paddingBottom">2dip</item>
        <item name="android:textColor">@drawable/main_home_tab_text_color</item>
        <item name="android:textSize">14sp</item>
        <item name="android:singleLine">true</item>
        <item name="android:background">@android:color/transparent</item>
    </style>-->

    <!-- 轨迹进度条样式 -->
   <!-- <style name="Widget.SeekBar.Normal" parent="@android:style/Widget.SeekBar">
        <item name="android:maxHeight">4.0dip</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">@android:drawable/progress_indeterminate_horizontal</item>
        <item name="android:progressDrawable">@drawable/seek_bar</item>
        <item name="android:minHeight">4.0dip</item>
        <item name="android:thumb">@mipmap/schedule_round_blue</item>
        <item name="android:thumbOffset">15.0dip</item>
    </style>-->

    <!-- RadioButton日期dialog 样式 -->
    <!--<style name="date_dialog">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:drawablePadding">5dp</item>
        <item name="android:button">@null</item>
        <item name="android:drawableLeft">@drawable/select</item>
        <item name="android:textColor">@drawable/dialog_text_color</item>
        <item name="android:textSize">13sp</item>
    </style>-->
</resources>
